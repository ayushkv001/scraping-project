
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Instagram Profile Scraper</title>
    <link href="stylesheet.css" rel="stylesheets">
  </head>
  <body style="height:100vh;display: flex;justify-content: center;color:green;font-weight: bold;background: linear-gradient(lightblue, lightgreen);">
    <form action="/getprofile" method="POST">
      <input type="text" name="username" id="" />
      <input type="submit" value="Get Details" />
    </form>
    <% if(data) {%>
    <div id="result">
      <h3>Total followers: <%=data.followers%></h3>
      <h3>likes: <%=data.lastposts[0].likes%></h3>
      <h3>comments: <%=data.lastposts[0].comments%></h3>
      <h3>Engagement rate: <%= (data.lastposts[0].likes + data.lastposts[0].comments)/(data.followers)*100%></h3>
    </div>
    <%}%>
  </body>
</html>